{
    "apiVersion": "v1",
    "kind": "Secret",
    "metadata": {
        "name": "ssl-certificates"
    },
    "data": {
        "cert.pem": "{{CERT}}", 
        "fullchain.pem": "{{FULL_CHAIN}}", 
        "privkey.pem": "{{PRIVKEY}}"
    }
}
